MusFy 0.1.5 corrige o texto quebrado no desktop e deixa o fluxo de atualização mais confiável no Windows.

- Repara automaticamente strings com mojibake no updater, nas notas de release e em dados dinâmicos vindos do backend.
- Corrige textos corrompidos como `versÃ£o`, `atualizaÃ§Ã£o` e separadores `Â·` em diferentes pontos da interface.
- Normaliza payloads de músicas, playlists, dispositivos, logs e status de update antes de exibir ou persistir no app.
- Fecha mini player, tray e backend gerenciado antes de chamar o instalador do update para evitar instalação incompleta.
- Dispara o `quitAndInstall` em modo silencioso compatível com o NSIS usado pelo MusFy, reduzindo a necessidade de reinstalar manualmente.
