Correcao estrutural do runtime local do desktop.

- bootstrap de SQLite e Redis agora acontece de forma independente, sem derrubar o cache quando o banco auxiliar falhar
- Redis embarcado passa a detectar queda apos a inicializacao e tenta se religar nas proximas operacoes
- health do backend agora expõe erro real de SQLite ou Redis para o desktop decidir corretamente quando reutilizar o servico
- mantidos os fixes anteriores de escrita atomica do database.json, cookies automaticos para yt-dlp e recarga da janela branca
