MusFy 0.1.11

- Corrige o botão `Reiniciar e instalar` no Windows.
- O updater agora usa o fluxo nativo `quitAndInstall` do `electron-updater` para instaladores NSIS.
- Remove o encerramento manual que fazia o app sumir sem abrir corretamente o instalador da atualização.
